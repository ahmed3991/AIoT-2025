# TP4 Models

Place the pickled scikit-learn pipelines generated in TP2 inside this directory.

Expected filenames:

- `lr_pipeline.pkl`
- `xgb_pipeline.pkl`

The MQTT subscriber (`ai_logic/mqtt_ai_subscriber.py`) loads these files at runtime.
